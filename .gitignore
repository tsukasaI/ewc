# Build
/target

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Nix
result
.direnv/
# git-hooks.nix regenerates this symlink into /nix/store on every
# `nix develop`; it's machine-specific and shouldn't be tracked (#74)
.pre-commit-config.yaml
